import { createTheme } from "@mui/material/styles";

export const uecsTheme = createTheme({
  palette: {
    primary: {
      main: "#74a64c",
      light: "#D0E0DE",
      contrastText: "#fff",
    },
    secondary: {
      main: "#60998E",
      contrastText: "#fff",
    },
  },
});
